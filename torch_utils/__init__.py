# This directory should contain files from the CB-AE repo.
# Run: python setup.py --copy-vendor /path/to/posthoc-generative-cbm
raise ImportError(
    "torch_utils/ is empty. Copy it from the CB-AE repo.\n"
    "Run: python setup.py --copy-vendor /path/to/posthoc-generative-cbm"
)
